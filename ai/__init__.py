"""
BitwiseHide AI training package (Phase 2.8).

Contains the training-time components only: dataset preparation, label
generation, the suitability CNN, and training/experiment code. Nothing in this
package is imported by the production backend; the backend consumes trained
artifacts through ``app.ml`` and ``app.core``.
"""
