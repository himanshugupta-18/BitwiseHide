[pytest]
# Tests live at the repo root (./tests) while the application package is
# under ./backend — make `app` importable and enable async tests so a plain
# `pytest` from the repo root works.
asyncio_mode = auto
pythonpath = backend
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
